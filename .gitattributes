# Normalize all text files to LF inside the repository.
* text=auto eol=lf

# Files executed inside Linux containers must keep LF endings.
*.sh text eol=lf
Dockerfile text eol=lf
Dockerfile.dev text eol=lf

# Binary assets — never normalize.
*.png binary
*.jpg binary
*.ico binary
*.woff binary
*.woff2 binary
