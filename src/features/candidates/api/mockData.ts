import type {
  AIAnalysis,
  Candidate,
  CandidateApplication,
  CandidateNote,
  OtherApplication,
} from "../types";

// Mock data removed — all data comes from the backend.

export const MOCK_CANDIDATES: Record<string, Candidate> = {};
export const MOCK_APPLICATIONS: Record<string, CandidateApplication> = {};
export const MOCK_AI_ANALYSES: Record<string, AIAnalysis> = {};
export const MOCK_NOTES: Record<string, CandidateNote[]> = {};
export const MOCK_OTHER_APPLICATIONS: Record<string, OtherApplication[]> = {};
